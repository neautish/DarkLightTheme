/** @type {import('next').NextConfig} */
const nextConfig = {
	images: {
		domains: ["source.unsplash.com", "unsplash.com", "images.unsplash.com", "plus.unsplash.com"],
	},
};

module.exports = nextConfig;
